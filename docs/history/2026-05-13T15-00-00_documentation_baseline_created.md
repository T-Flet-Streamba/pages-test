# Documentation baseline created

The timestamp prefix reflects **creation time of the documentation work** (§6.2 option 2 in the maintainer prompt), not a git merge commit.

## Summary

Initial `docs/` tree was added: `product/` (overview, usage, integrations, six `feature_*` files), `technical/` (`project_layout.md`, `architecture.md`, `environment_variables.md`, `testing.md`), `future/` (one draft idea), and `prompts/` populated by copying the standard maintainer and consumer prompt files from `.prompts/`.

## Pull request

https://github.com/streamba/vor-collabgpt-functions/pull/371
(It also includes a follow-up updating of those docs mentioned in docs\history\2026-05-07T12-11-16_vor_search_new_indexes.md)

## Areas touched

- `docs/**` (new)
- `README.md` (root pointer to canonical `docs/`)

## Feature numbers referenced

1–6 as defined in `docs/product/feature_*.md`.
